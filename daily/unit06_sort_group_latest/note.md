## daily lesson note template
# Day 6

## 오늘의 목표

- 장비의 최신 측정값과 현재 상태값 다루기

## 구현할 함수 목록 

- group_by_sensor
- find_latest_measurement
- build_latest_state


## 핵심 개념

- `sorted()`는 새 리스트 생성,  `list.sort()`는 원본 수정
- `max()`로 최신값 찾기 - `max(targetList, key=lambda x: x[targetKey])`
- `dict.setdefault(key, []).append(value)` 키가 이미 존재하면 그 키에 새 데이터를 추가하고, 아니라면, 빈 배열 새로만들어서 추가
  
## 발견한 문제

- 

## 오늘의 결론